<h1>Welcome to Yelp - Your Restaurant Guide</h1>

<% @restaurants.each do |restaurant| %>
  <div>
    <h3><%= restaurant.name %></h3>
    <p><%= restaurant.address %></p>
    <p><%= restaurant.category %></p>
    <%= link_to 'See details', restaurant_path(restaurant) %>
  </div>
<% end %>
